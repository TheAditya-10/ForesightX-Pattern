Commands
========

The Makefile contains the central entry points for common tasks related to this project.

Training
^^^^^^^^

``make train`` runs the package training entry point.

Serving
^^^^^^^

``make serve_api`` starts the FastAPI service with Uvicorn.

Testing
^^^^^^^

``make test`` runs the repository test suite.
