stages:
  foundation_training:
    cmd: python3 -m foresightx_pattern.ml.training.train
    deps:
      - foresightx_pattern
      - configs/default.yaml
    params: []
    outs:
      - artifacts/data
      - artifacts/model
      - artifacts/reports
